---
import Layout from "../layouts/Layout.astro";
import Heading from "../components/Heading.astro";
import { Size } from "../types";
import { Image } from "astro:assets";
import heroImage from "../images/hero.webp";
import config from "../../config";
---

<Layout title={config.title} description={config.description}>
  <section class="py-16">
    <div class="container grid grid-cols-2 gap-8 mx-auto items-center">
      <article class="space-y-12 max-w-prose text-violet-950">
        <Heading size={Size.XL}>
          Společně na cestě za zdravím vašich dětí
        </Heading>
        <p class="text-orange-950 leading-loose">
          {config.description}
        </p>
        <a
          href="#"
          class="inline-flex bg-amber-400 text-purple-950 font-bold text-lg rounded-full py-4 px-10 hover:bg-amber-300 focus:bg-amber-300 transition-all"
        >
          Kontaktujte nás
        </a>
      </article>
      <Image src={heroImage} alt="Roztomilá holčička" />
    </div>
  </section>
  <section class="bg-gray-50">
    <article
      class="-translate-y-12 shadow-2xl shadow-gray-200 max-w-3xl mx-auto p-12 rounded-3xl bg-gray-50 text-center text-purple-950"
    >
      <Heading size={Size.SM} as="p">
        Soucitnou péči a osobním přístupem se snažíme zajistit, aby každá
        návštěva byla co nejpříjemnější a nejužitečnější pro celou rodinu.
      </Heading>
    </article>
  </section>
</Layout>
