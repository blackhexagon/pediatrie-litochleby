---
import Layout from "../layouts/Layout.astro";
import config from "../../config";
import Intro from "../components/Intro.astro";
---

<Layout
  title={config.links.vaccination.title}
  description={config.links.vaccination.description}
>
  <Intro key="vaccination" />

  <!-- Flu Vaccination Section -->
  <section class="bg-amber-50 py-16">
    <div class="container mx-auto px-4">
      <div
        class="max-w-xl mx-auto p-8 bg-white rounded-lg shadow-lg overflow-hidden space-y-4"
      >
        <h2
          class="text-center text-2xl font-bold text-amber-600 flex items-center justify-center"
        >
          OČKOVÁNÍ PROTI CHŘIPCE
        </h2>
        <span class="block text-center text-3xl">💉</span>
        <p class="font-semibold text-gray-700">
          Od 1. října je dostupné očkování proti chřipce.
        </p>

        <div class="bg-amber-50 p-4 rounded-lg">
          <h3 class="font-semibold text-amber-800 mb-2">Pro menší děti:</h3>
          <p>
            Nabízíme nosní vakcínu, která je šetrnější alternativou pro děti,
            které se bojí injekcí.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- RSV Vaccination Section -->
  <section class="bg-blue-50 py-16">
    <div class="container mx-auto px-4">
      <div
        class="max-w-xl mx-auto p-8 bg-white rounded-lg shadow-lg overflow-hidden space-y-4"
      >
        <h2
          class="text-center text-2xl font-bold text-blue-600 flex items-center"
        >
          OCHRANA VAŠEHO DÍTĚTE PŘED RSV BEYFORTUS
        </h2>
        <span class="block text-center text-3xl">🛡️</span>
        <p class="font-semibold text-gray-700">
          Přípravek Beyfortus je indikován k prevenci onemocnění dolních cest
          dýchacích respiračním syncytiálním virem (RSV) u novorozenců a
          kojenců.
        </p>

        <div class="bg-blue-50 p-4 rounded-lg">
          <h3 class="font-semibold text-blue-800 mb-2">Způsob aplikace:</h3>
          <p>
            Očkuje se obvykle jednou dávkou před první RSV sezónou (listopad až
            březen), při narození dítěte v RSV sezóně pak co nejdříve po
            narození, intramuskulárně.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-gray-50 py-16 grow">
    <div
      class="container infogram-embed"
      data-id="_/rQaO5L9yPVzC9y47IKiz"
      data-type="interactive"
      data-title="Očkovací kalendář pro děti"
    >
    </div>
    <script>
      !(function (e, n, i, s) {
        var d = "InfogramEmbeds";
        var o = e.getElementsByTagName(n)[0];
        if (window[d] && window[d].initialized)
          window[d].process && window[d].process();
        else if (!e.getElementById(i)) {
          var r = e.createElement(n);
          (r.async = 1),
            (r.id = i),
            (r.src = s),
            o.parentNode.insertBefore(r, o);
        }
      })(
        document,
        "script",
        "infogram-async",
        "https://e.infogram.com/js/dist/embed-loader-min.js",
      );
    </script>
  </section>
</Layout>
